import {app,BrowserWindow,ipcMain,dialog,Tray,Menu,nativeImage,Notification,shell} from 'electron';
import http from 'node:http';
import net from 'node:net';
import crypto from 'node:crypto';
import {spawn,spawnSync} from 'node:child_process';
import fs from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
import researchHandler from './app/api/research.js';
import inspectHandler from './app/api/inspect.js';

const __dirname=path.dirname(fileURLToPath(import.meta.url));
const appDir=path.join(__dirname,'app');
app.setAppUserModelId('com.atlanex.researchos');
let mainWindow=null,server=null,serverPort=0,tray=null,monitorTimer=null,sidecarProc=null,sidecarPort=0,isQuitting=false;
const pendingLLM=new Map();
const INTERNAL_HOST='127.0.0.1';
const INTERNAL_MODEL='atlas-auto';
const MIME={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.json':'application/json; charset=utf-8','.webmanifest':'application/manifest+json','.png':'image/png','.svg':'image/svg+xml'};

const wait=ms=>new Promise(r=>setTimeout(r,ms));
function freePort(){return new Promise((resolve,reject)=>{const s=net.createServer();s.unref();s.on('error',reject);s.listen(0,INTERNAL_HOST,()=>{const p=s.address().port;s.close(()=>resolve(p))})})}
function sideDir(){return app.isPackaged?path.join(process.resourcesPath,'agent-engine'):path.join(__dirname,'sidecar')}
function agentExe(){return process.platform==='win32'?path.join(sideDir(),'atlas-agent-engine.exe'):path.join(sideDir(),'atlas-agent-engine')}
function devPython(){return process.platform==='win32'?path.join(sideDir(),'.venv','Scripts','python.exe'):path.join(sideDir(),'.venv','bin','python')}

async function startSidecar(){
 if(sidecarProc&&!sidecarProc.killed)return true;
 sidecarPort=await freePort();
 let cmd,args;
 if(fs.existsSync(agentExe())){cmd=agentExe();args=['--port',String(sidecarPort)]}
 else if(fs.existsSync(devPython())){cmd=devPython();args=[path.join(sideDir(),'server.py'),'--port',String(sidecarPort)]}
 else return false;
 sidecarProc=spawn(cmd,args,{cwd:sideDir(),windowsHide:true,env:{...process.env,ATLAS_LLM_BASE_URL:'http://'+INTERNAL_HOST+':'+serverPort+'/v1',ATLAS_MODEL:INTERNAL_MODEL},stdio:['ignore','pipe','pipe']});
 sidecarProc.on('exit',()=>{sidecarProc=null});
 for(let i=0;i<80;i++){await wait(250);try{const r=await fetch('http://'+INTERNAL_HOST+':'+sidecarPort+'/health');if(r.ok)return true}catch{}}
 try{sidecarProc?.kill()}catch{};sidecarProc=null;return false
}
async function sidecar(route,options={}){if(!sidecarProc&&!(await startSidecar()))throw new Error('Atlas Agent Engine no disponible');const r=await fetch('http://'+INTERNAL_HOST+':'+sidecarPort+route,options);const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||('sidecar HTTP '+r.status));return j}

function rendererLLM(payload){return new Promise((resolve,reject)=>{if(!mainWindow||mainWindow.isDestroyed())return reject(new Error('Renderer no disponible'));const id=crypto.randomUUID(),timer=setTimeout(()=>{pendingLLM.delete(id);reject(new Error('LLM bridge timeout'))},300000);pendingLLM.set(id,{resolve,reject,timer});mainWindow.webContents.send('atlas:llm-request',{id,...payload})})}
function hashEmbedding(text,dim=384){const v=new Array(dim).fill(0),t=String(text||'').toLowerCase().match(/[\p{L}\p{N}_-]+/gu)||[];for(const token of t){let h=2166136261;for(let i=0;i<token.length;i++){h^=token.charCodeAt(i);h=Math.imul(h,16777619)}v[(h>>>0)%dim]+=((h>>>8)&1)?1:-1}const n=Math.sqrt(v.reduce((s,x)=>s+x*x,0))||1;return v.map(x=>x/n)}
function adapter(handler,u,req,res){req.query=Object.fromEntries(u.searchParams.entries());let status=200;const o={status(n){status=n;return o},setHeader(k,v){res.setHeader(k,v);return o},json(x){res.statusCode=status;res.setHeader('Content-Type','application/json; charset=utf-8');res.end(JSON.stringify(x));return o},end(x=''){res.statusCode=status;res.end(x);return o}};return handler(req,o)}
function serveStatic(res,u){let pn=decodeURIComponent(u.pathname);if(pn==='/')pn='/index.html';const file=path.normalize(path.join(appDir,pn));if(!file.startsWith(appDir)){res.statusCode=403;return res.end('Forbidden')}fs.stat(file,(e,st)=>{if(e||!st.isFile()){res.statusCode=404;return res.end('Not found')}res.setHeader('Content-Type',MIME[path.extname(file).toLowerCase()]||'application/octet-stream');fs.createReadStream(file).pipe(res)})}
async function startServer(){if(server)return serverPort;return await new Promise((resolve,reject)=>{server=http.createServer(async(req,res)=>{const u=new URL(req.url,'http://127.0.0.1');try{
 if(u.pathname==='/api/research')return await adapter(researchHandler,u,req,res);
 if(u.pathname==='/api/inspect')return await adapter(inspectHandler,u,req,res);
 if(u.pathname==='/v1/models'){res.setHeader('Content-Type','application/json');return res.end(JSON.stringify({object:'list',data:[{id:INTERNAL_MODEL,object:'model'}]}))}
 if(u.pathname==='/v1/embeddings'&&req.method==='POST'){let raw='';for await(const c of req)raw+=c;const b=JSON.parse(raw||'{}'),arr=Array.isArray(b.input)?b.input:[b.input||''];res.setHeader('Content-Type','application/json');return res.end(JSON.stringify({object:'list',data:arr.map((x,i)=>({object:'embedding',index:i,embedding:hashEmbedding(x)})),model:b.model||'atlas-feature-hash-384'}))}
 if(u.pathname==='/v1/chat/completions'&&req.method==='POST'){let raw='';for await(const c of req)raw+=c;const b=JSON.parse(raw||'{}'),model=String(b.model||INTERNAL_MODEL).replace(/^openai[/:]/,'');const reply=await rendererLLM({messages:b.messages||[],model});res.setHeader('Content-Type','application/json');return res.end(JSON.stringify({id:'atlas-'+Date.now(),object:'chat.completion',created:Math.floor(Date.now()/1000),model,choices:[{index:0,message:{role:'assistant',content:reply.text},finish_reason:'stop'}]}))}
 return serveStatic(res,u)
 }catch(e){res.statusCode=500;res.setHeader('Content-Type','application/json');res.end(JSON.stringify({error:String(e?.message||e)}))}});server.listen(0,INTERNAL_HOST,()=>{serverPort=server.address().port;resolve(serverPort)});server.on('error',reject)})}

function monitorPath(){return path.join(app.getPath('userData'),'atlas-monitors.json')}
const defaultMonitor=()=>({enabled:true,startWithWindows:false,jobs:[],events:[]});
function loadMonitor(){try{const x=JSON.parse(fs.readFileSync(monitorPath(),'utf8'));return{...defaultMonitor(),...x,jobs:Array.isArray(x.jobs)?x.jobs:[],events:Array.isArray(x.events)?x.events:[]}}catch{return defaultMonitor()}}
function saveMonitor(x){try{fs.mkdirSync(path.dirname(monitorPath()),{recursive:true});fs.writeFileSync(monitorPath(),JSON.stringify(x,null,2));return true}catch{return false}}
function cleanJob(j){try{const u=new URL(String(j?.url||''));if(!['http:','https:'].includes(u.protocol))return null;return{id:String(j.id||crypto.randomUUID()),title:String(j.title||u.href).slice(0,180),url:u.href,intervalMinutes:Math.max(15,Math.min(10080,Number(j.intervalMinutes)||360)),enabled:j.enabled!==false,lastChecked:Number(j.lastChecked)||0,nextCheck:Number(j.nextCheck)||0,lastHash:String(j.lastHash||''),lastTitle:String(j.lastTitle||''),changed:!!j.changed,failures:Number(j.failures)||0,lastError:j.lastError||null,history:Array.isArray(j.history)?j.history.slice(-30):[]}}catch{return null}}
async function scanMonitor(url){const r=await fetch('http://'+INTERNAL_HOST+':'+serverPort+'/api/inspect?url='+encodeURIComponent(url));const j=await r.json().catch(()=>({})),s=j.snapshot||{},h=String(s.contentHash||j.contentHash||'');if(!r.ok)throw new Error(j.error||('HTTP '+r.status));if(!/^[a-f0-9]{64}$/i.test(h))throw new Error('SHA-256 ausente');return{hash:h,title:s.title||j.title||url,fetchedAt:s.fetchedAt||j.fetchedAt||new Date().toISOString()}}
async function monitorTick(force=false){const s=loadMonitor();if(!s.enabled&&!force)return s;for(const job of s.jobs){if(!job.enabled||(!force&&job.nextCheck>Date.now()))continue;const now=Date.now();try{const before=job.lastHash||'',snap=await scanMonitor(job.url);Object.assign(job,{lastChecked:now,nextCheck:now+job.intervalMinutes*60000,lastHash:snap.hash,lastTitle:snap.title,changed:!!before&&before!==snap.hash,failures:0,lastError:null});job.history=[...(job.history||[]),{at:now,hash:snap.hash,previousHash:before||null,changed:job.changed,fetchedAt:snap.fetchedAt}].slice(-30);if(job.changed&&Notification.isSupported())new Notification({title:'Atlanex · cambio detectado',body:job.title}).show()}catch(e){job.lastChecked=now;job.failures=(job.failures||0)+1;job.lastError=String(e?.message||e);job.nextCheck=now+Math.min(job.intervalMinutes,60)*60000}}saveMonitor(s);return s}
function startMonitor(){clearInterval(monitorTimer);monitorTimer=setInterval(()=>monitorTick(false).catch(()=>{}),60000);setTimeout(()=>monitorTick(false).catch(()=>{}),6000)}
function setLogin(v){try{app.setLoginItemSettings({openAtLogin:!!v,args:v?['--background']:[]});return app.getLoginItemSettings().openAtLogin}catch{return false}}

function createWindow(show=true){mainWindow=new BrowserWindow({width:1440,height:900,minWidth:1050,minHeight:680,frame:false,show:false,backgroundColor:'#02050a',webPreferences:{preload:path.join(__dirname,'preload.cjs'),contextIsolation:true,nodeIntegration:false,sandbox:true}});mainWindow.loadURL('http://'+INTERNAL_HOST+':'+serverPort+'/');mainWindow.webContents.setWindowOpenHandler(({url})=>{try{const u=new URL(url);if(u.hostname.endsWith('puter.com')||u.hostname.endsWith('puter.site'))return{action:'allow'};shell.openExternal(url)}catch{}return{action:'deny'}});if(show)mainWindow.once('ready-to-show',()=>mainWindow.show());mainWindow.on('close',e=>{if(!isQuitting&&process.platform!=='darwin'){e.preventDefault();mainWindow.hide()}});mainWindow.on('closed',()=>{mainWindow=null});return mainWindow}
function createTray(){try{const p=path.join(appDir,'icons','icon-192.png');if(!fs.existsSync(p))return;tray=new Tray(nativeImage.createFromPath(p).resize({width:18,height:18}));tray.setToolTip('Atlanex');tray.setContextMenu(Menu.buildFromTemplate([{label:'Abrir Atlas',click:()=>{mainWindow?.show();mainWindow?.focus()}},{label:'Revisar monitores ahora',click:()=>monitorTick(true).catch(()=>{})},{type:'separator'},{label:'Salir completamente',click:()=>{isQuitting=true;app.quit()}}]));tray.on('double-click',()=>mainWindow?.show())}catch{}}
function acceptanceLog(payload){try{const p=process.env.ATLAS_ACCEPTANCE_LOG;if(p)fs.writeFileSync(p,JSON.stringify(payload,null,2),'utf8')}catch{}}
async function rendererCheck(win){
 const consoleErrors=[],preloadErrors=[];
 win.webContents.on('console-message',(_e,level,message)=>{if(level>=2)consoleErrors.push(String(message).slice(0,500))});
 win.webContents.on('preload-error',(_e,preloadPath,error)=>preloadErrors.push({preloadPath,error:String(error?.message||error)}));
 await win.loadURL('http://'+INTERNAL_HOST+':'+serverPort+'/');
 await win.webContents.executeJavaScript("document.readyState==='complete'?true:new Promise(r=>addEventListener('load',()=>r(true),{once:true}))");
 return await win.webContents.executeJavaScript(`(()=>{const fatal=document.getElementById('atlasFatal');const fatalText=document.getElementById('atlasFatalText');const body=(document.body?.innerText||'').toLowerCase();return{atlas:(document.title||'').toLowerCase().includes('internet atlas')||body.includes('internet atlas'),title:document.title||'',splash:!!document.getElementById('splash'),machine:!!document.getElementById('launchMachine'),fatal:!!fatal&&getComputedStyle(fatal).display!=='none',fatalText:fatalText?.textContent||'',desktop:!!globalThis.atlasDesktop?.isDesktop}})()`).then(x=>({...x,consoleErrors,preloadErrors}))
}

async function runSmoke(){let win;try{await startServer();win=createWindow(false);const r=await rendererCheck(win);if(!r.atlas||!r.splash||!r.machine||r.fatal||!r.desktop)throw new Error('renderer/preload smoke failed '+JSON.stringify(r));if(app.isPackaged){const x=spawnSync(agentExe(),['--self-test'],{encoding:'utf8',timeout:120000,windowsHide:true});if(x.status!==0)throw new Error('agent self-test failed '+(x.stderr||x.stdout||''));if(!fs.existsSync(path.join(sideDir(),'THIRD_PARTY_PYTHON_LICENSES.json')))throw new Error('license inventory missing')}console.log(JSON.stringify({ok:true,renderer:true,preload:true}));return 0}catch(e){console.error(JSON.stringify({ok:false,error:String(e?.message||e)}));return 1}finally{try{win?.destroy()}catch{};try{server?.close();server=null}catch{}}}
async function runAcceptance(){let backup=null,had=false,win=null;try{await startServer();win=createWindow(false);const renderer=await rendererCheck(win);if(!renderer.atlas||!renderer.machine||renderer.fatal||!renderer.desktop)throw new Error('renderer/preload acceptance failed '+JSON.stringify(renderer));const base='http://'+INTERNAL_HOST+':'+serverPort;const rr=await fetch(base+'/api/research?q='+encodeURIComponent('OpenAI API official documentation'),{signal:AbortSignal.timeout(90000)}),research=await rr.json();const providers=new Set((research.diagnostics||[]).filter(x=>x.ok).map(x=>x.provider)).size;if(!rr.ok||!(research.results||[]).length||providers<3)throw new Error('live research acceptance failed');const proofUrl='https://en.wikipedia.org/wiki/OpenAI',ir=await fetch(base+'/api/inspect?url='+encodeURIComponent(proofUrl),{signal:AbortSignal.timeout(90000)}),ins=await ir.json(),snap=ins.snapshot||{};if(!ir.ok||!/^[a-f0-9]{64}$/i.test(String(snap.contentHash||ins.contentHash||''))||String(snap.text||ins.text||'').length<500)throw new Error('live source snapshot/hash acceptance failed');const mp=monitorPath();had=fs.existsSync(mp);if(had)backup=fs.readFileSync(mp);const st=defaultMonitor(),job=cleanJob({id:'acceptance-live-source',title:'Acceptance source',url:proofUrl,intervalMinutes:15});st.jobs=[job];if(!saveMonitor(st))throw new Error('monitor persistence write failed');await monitorTick(true);const loaded=loadMonitor().jobs[0];if(!loaded?.lastHash||!loaded.lastChecked||loaded.failures||!loaded.history?.length)throw new Error('monitor persistence live check failed');if(!app.isPackaged)throw new Error('acceptance must run packaged');const ag=spawnSync(agentExe(),['--self-test-deep'],{encoding:'utf8',timeout:300000,windowsHide:true});let payload={};try{payload=JSON.parse((ag.stdout||'').trim().split(/\r?\n/).filter(Boolean).at(-1)||'{}')}catch{}if(ag.status!==0||!payload.ok||!payload.crewai_runtime||!payload.gpt_researcher_runtime||!payload.gpt_researcher_deep){const detail=JSON.stringify({status:ag.status,payload,stderr:String(ag.stderr||'').slice(-8000),stdout:String(ag.stdout||'').slice(-8000)});throw new Error('agent deep acceptance failed '+detail)}if(!fs.existsSync(path.join(sideDir(),'THIRD_PARTY_PYTHON_LICENSES.json'))||!fs.existsSync(path.join(sideDir(),'THIRD_PARTY_NOTICES.md')))throw new Error('packaged legal inventory missing');const result={ok:true,renderer,liveResearch:{results:research.results.length,providers},snapshot:{sha256:snap.contentHash||ins.contentHash},monitor:{persisted:true},agentEngine:{crewai:true,gptResearcher:true,deep:true}};acceptanceLog(result);console.log(JSON.stringify(result));return 0}catch(e){const result={ok:false,error:String(e?.message||e),stack:String(e?.stack||'').slice(-4000)};acceptanceLog(result);console.error(JSON.stringify(result));return 1}finally{try{win?.destroy()}catch{};try{const mp=monitorPath();if(had&&backup)fs.writeFileSync(mp,backup);else if(!had&&fs.existsSync(mp))fs.unlinkSync(mp)}catch{};try{server?.close();server=null}catch{}}}

const lock=app.requestSingleInstanceLock();if(!lock)app.quit();else{app.on('second-instance',()=>{mainWindow?.show();mainWindow?.focus()});app.whenReady().then(async()=>{if(process.argv.includes('--acceptance-test')){isQuitting=true;return app.exit(await runAcceptance())}if(process.argv.includes('--smoke-test')){isQuitting=true;return app.exit(await runSmoke())}await startServer();createWindow(true);createTray();startMonitor();const m=loadMonitor();if(m.startWithWindows)setLogin(true);if(process.argv.includes('--background'))mainWindow?.hide();if(fs.existsSync(agentExe())||fs.existsSync(devPython()))startSidecar().catch(()=>{})});app.on('activate',()=>{if(!mainWindow)createWindow();else{mainWindow.show();mainWindow.focus()}});app.on('window-all-closed',()=>{});app.on('before-quit',()=>{isQuitting=true;clearInterval(monitorTimer);try{server?.close()}catch{};try{sidecarProc?.kill()}catch{}})}

ipcMain.handle('atlas:window',(_e,a)=>{if(!mainWindow)return false;if(a==='minimize')mainWindow.minimize();else if(a==='maximize')mainWindow.isMaximized()?mainWindow.unmaximize():mainWindow.maximize();else if(a==='close')mainWindow.close();else if(a==='fullscreen')mainWindow.setFullScreen(!mainWindow.isFullScreen());return true});
ipcMain.handle('atlas:save-project',async(_e,{text,defaultName})=>{const x=await dialog.showSaveDialog(mainWindow,{title:'Guardar proyecto Atlas',defaultPath:path.join(app.getPath('documents'),defaultName||'Atlas_Project.atlas.json'),filters:[{name:'Atlas Project',extensions:['json']}]});if(x.canceled||!x.filePath)return false;fs.writeFileSync(x.filePath,text,'utf8');return true});
ipcMain.handle('atlas:open-project',async()=>{const x=await dialog.showOpenDialog(mainWindow,{title:'Abrir proyecto Atlas',properties:['openFile'],filters:[{name:'Atlas Project',extensions:['json']}]});if(x.canceled||!x.filePaths[0])return null;return{path:x.filePaths[0],text:fs.readFileSync(x.filePaths[0],'utf8')}});
ipcMain.handle('atlas:notify',(_e,{title,body})=>{if(Notification.isSupported())new Notification({title:title||'Atlanex',body:body||''}).show();return true});
ipcMain.on('atlas:llm-response',(_e,p)=>{const x=pendingLLM.get(p.id);if(!x)return;clearTimeout(x.timer);pendingLLM.delete(p.id);p.ok?x.resolve(p):x.reject(new Error(p.error||'LLM bridge error'))});
ipcMain.handle('atlas:agent-health',async()=>{try{return await sidecar('/health')}catch(e){return{ready:false,message:String(e?.message||e)}}});
ipcMain.handle('atlas:agent-doctor',async()=>{try{return await sidecar('/doctor')}catch(e){return{ready:false,error:String(e?.message||e)}}});
ipcMain.handle('atlas:agent-smoke',async()=>{try{return await sidecar('/smoke',{method:'POST'})}catch(e){return{ok:false,error:String(e?.message||e)}}});
ipcMain.handle('atlas:agent-repair',async()=>{try{sidecarProc?.kill()}catch{};sidecarProc=null;return await startSidecar()});
ipcMain.handle('atlas:agent-setup',async()=>await startSidecar());
ipcMain.handle('atlas:agent-setup-status',()=>({running:false,done:!!sidecarProc,ok:!!sidecarProc,progress:sidecarProc?100:0,line:sidecarProc?'Agent Engine listo':'No iniciado'}));
ipcMain.handle('atlas:agent-start',async(_e,p)=>await sidecar('/runs',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(p)}));
ipcMain.handle('atlas:agent-run',async(_e,id)=>await sidecar('/runs/'+encodeURIComponent(id)));
ipcMain.handle('atlas:agent-cancel',async(_e,id)=>await sidecar('/runs/'+encodeURIComponent(id)+'/cancel',{method:'POST'}));
ipcMain.handle('atlas:open-evidence',async(_e,{url})=>{try{const u=new URL(url);if(!['http:','https:'].includes(u.protocol))return false;const w=new BrowserWindow({width:1180,height:820,parent:mainWindow||undefined,webPreferences:{contextIsolation:true,nodeIntegration:false,sandbox:true}});w.loadURL(u.href);return true}catch{return false}});
ipcMain.handle('atlas:monitor-list',()=>loadMonitor());
ipcMain.handle('atlas:monitor-sync',async(_e,p)=>{const old=loadMonitor(),map=new Map(old.jobs.map(j=>[j.id,j])),jobs=[];for(const raw of(p?.jobs||[])){const j=cleanJob(raw);if(!j)continue;const x=map.get(j.id);jobs.push(x?{...j,lastChecked:x.lastChecked,lastHash:x.lastHash,lastTitle:x.lastTitle,changed:x.changed,failures:x.failures,lastError:x.lastError,history:x.history,nextCheck:x.nextCheck}:j)}old.jobs=jobs;saveMonitor(old);return loadMonitor()});
ipcMain.handle('atlas:monitor-run-now',async()=>await monitorTick(true));
ipcMain.handle('atlas:monitor-config',(_e,p)=>{const s=loadMonitor();if(typeof p?.enabled==='boolean')s.enabled=p.enabled;if(typeof p?.startWithWindows==='boolean'){s.startWithWindows=p.startWithWindows;setLogin(s.startWithWindows)}saveMonitor(s);return s});
ipcMain.handle('atlas:pick-research-files',async()=>{const r=await dialog.showOpenDialog(mainWindow,{title:'Añadir documentos a Atlanex',properties:['openFile','multiSelections'],filters:[{name:'Documentos',extensions:['pdf','txt','md','json','html','htm','csv','xml']}]});if(r.canceled)return[];const out=[];for(const file of r.filePaths.slice(0,12)){try{const st=fs.statSync(file);if(st.size>20000000){out.push({name:path.basename(file),error:'Archivo mayor de 20 MB'});continue}const ext=path.extname(file).toLowerCase();let text='',kind='text';if(ext==='.pdf'){const{PDFParse}=await import('pdf-parse');const parser=new PDFParse({data:new Uint8Array(fs.readFileSync(file))});try{text=String((await parser.getText())?.text||'');kind='pdf'}finally{try{await parser.destroy()}catch{}}}else{text=fs.readFileSync(file,'utf8');if(ext==='.html'||ext==='.htm')text=text.replace(/<script[\s\S]*?<\/script>/gi,' ').replace(/<style[\s\S]*?<\/style>/gi,' ').replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').trim()}text=text.slice(0,180000);if(!text.trim()){out.push({name:path.basename(file),error:'No se pudo extraer texto'});continue}out.push({name:path.basename(file),kind,size:st.size,text,contentHash:crypto.createHash('sha256').update(text,'utf8').digest('hex'),fetchedAt:new Date().toISOString()})}catch(e){out.push({name:path.basename(file),error:String(e?.message||e).slice(0,300)})}}return out});
ipcMain.handle('atlas:version',()=>app.getVersion());
